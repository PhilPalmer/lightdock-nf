params {
    pdb_paths = false
    swarms = 400
    glowworms = 300
    steps = 10
    outdir = 'results'
    help = false
}

profiles {
    test {
        params.pdb_paths = 'testdata/test_pdbs.csv'
        params.swarms = 10
        params.glowworms = 10
        params.steps = 10
    }
}